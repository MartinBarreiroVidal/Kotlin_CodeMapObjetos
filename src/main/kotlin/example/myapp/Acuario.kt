package example.myapp.example.myapp

class Acuario {
    var width: Int = 20
    var height: Int = 40
    var length: Int = 100

    fun printSize() {
        println("Ancho: $width cm " +
                "Largo: $length cm " +
                "Alto: $height cm ")

    }
}